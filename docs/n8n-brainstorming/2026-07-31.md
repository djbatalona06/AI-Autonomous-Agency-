# n8n Workflow Scout — 2026-07-31

Manual run — both scheduled Actions (`n8n-workflow-scout.yml`, `n8n-brainstorm-scrape.yml`)
have now failed every day since 2026-07-20 (12 consecutive days). See
`n8n-workflows/_inbox/2026-07-31-scrape-log.md` for the full diagnosis, including a
correction to PR #49's OIDC-token theory — the real, confirmed-in-logs root cause is the
Anthropic account's credit balance, not OIDC.

No manual catch-up ran between 2026-07-26 and 2026-07-30, so this run also closes that
six-day gap — one new entry per vertical (`B11`), prioritizing closing the diagnostic gap
over an exhaustive re-crawl.

Each entry below is import-ready as an n8n node chain, priced per the Rung 2 complexity
rubric ($1,500–$2,000 simple / $2,000–$3,500 medium / $3,500–$5,000 complex) where it's a
net-new build, or flagged as a reinforcement of an existing card.

---

## Sales

### 11. Inbound Multi-Channel AI Sales Agent with RAG + Booking — *new candidate*
`Webhook (Website Chat/WhatsApp/Instagram/Facebook)` → `Switch (normalize channel)` → `Vector Store lookup` → `AI Agent (RAG-grounded + booking intent)` → `CRM upsert` → `IF (booking intent)` → `Google Calendar/Cal.com` → `reply on channel`
- **Template fit:** new — every existing SAL card is outbound; this is the first always-on inbound agent, feeding into SAL-03 once a lead books.
- **Source:** ["Multi-platform AI sales agent with RAG, CRM logging & appointment booking"](https://n8n.io/workflows/4508-multi-platform-ai-sales-agent-with-rag-crm-logging-and-appointment-booking/)

---

## Ecommerce

### 11. Multi-Channel + Ad Retargeting Abandoned Cart Recovery — *reinforces ECM-01*
`Shopify Webhook` → `Wait (1hr/24hr/72hr)` → `IF (purchased?)` → `Switch (touch #)` → `Email + Twilio SMS + WhatsApp` → `HTTP Request (Meta Custom Audiences API)` → `Google Sheets (log)`
- **Template fit:** reinforces ECM-01 — adds a paid-media retargeting channel no existing ECM card touches; upsell for clients already running Meta ads.
- **Source:** ["Recover Shopify abandoned carts with email, SMS, WhatsApp & Facebook retargeting"](https://n8n.io/workflows/11805-recover-shopify-abandoned-carts-with-email-sms-whatsapp-and-facebook-retargeting/)

---

## Wholesaling / REI

### 11. Inbound Buyer Chatbot — AI Property Match + Auto-Scheduling — *new candidate*
`Chat/Webhook Trigger` → `AI Agent (parse buyer criteria)` → `HTTP Request (MLS/inventory query)` → `Code (match-score)` → `AI Agent (compose match summary)` → `Google Calendar` → `auto-book showing` → `CRM (log)`
- **Template fit:** new — every existing WHL card is seller-side/outbound; this opens a second, inbound buyer-facing product line.
- **Source:** ["Real estate chatbot with AI property matching and automated calendar scheduling"](https://n8n.io/workflows/7250-real-estate-chatbot-with-ai-property-matching-and-automated-calendar-scheduling/)

---

## Productivity

### 11. Pre-Meeting CRM Research + Daily Agenda Briefing — *reinforces PRD-02*
`Schedule Trigger` → `Google Calendar (today's meetings)` → `Loop Over Items` → `HTTP Request (Attio CRM history)` → `AI Agent (talking points)` → `Aggregate` → `Slack (daily agenda)`
- **Template fit:** reinforces PRD-02's mirror image — runs before a meeting, grounded in live CRM history, instead of after via transcript.
- **Source:** ["AI-powered meeting research & daily agenda with Google Calendar, Attio CRM, and Slack"](https://n8n.io/workflows/7968-ai-powered-meeting-research-and-daily-agenda-with-google-calendar-attio-crm-and-slack/)

---

## Small Business

### 11. RAG-Grounded Customer Support Knowledge-Base Agent — *reinforces SMB-06*
`Gmail Trigger` → `Google Drive (sync docs to vector store)` → `AI Agent (GPT-4o, RAG classify + reply)` → `Switch` → `Gmail (send/draft)` → `Slack (escalate)`
- **Template fit:** reinforces SMB-06 — grounds replies in synced docs instead of a static template, escalating only what the docs can't answer.
- **Source:** ["Smart Customer Support System with GPT-4o, Gmail, Slack & Drive Knowledge Base"](https://n8n.io/workflows/4543-smart-customer-support-system-with-gpt-4o-gmail-slack-and-drive-knowledge-base/)

---

## Quick take for DJ

- **Billing blocker, unresolved 12 days running:** both scheduled Actions need the
  Anthropic account's credit balance topped up — this is now the single blocker for either
  pipeline resuming unattended. See the scrape log for full run-history evidence.
- **PR #49 correction:** its OIDC-token diagnosis doesn't match the job logs (instant
  `is_error`, `$0.00` cost, no OIDC error text) — the `github_token` change is safe to merge
  but won't unblock anything by itself. Commented on the PR with this finding.
- **Fastest close:** Small Business #11 (RAG support agent) is the cheapest incremental
  build this round — a depth upgrade to an existing SMB-06 client's autoresponder, not a
  net-new sale.
- **Biggest opportunity:** Wholesaling/REI #11 and Sales #11 both open new *inbound*
  product lines where the whole existing catalog (in both verticals) is outbound-only.
