# n8n Workflow Scout — 2026-08-07

Manual run (see note below — both automated GitHub Actions failed again today). Source:
targeted web search (`site:n8n.io/workflows ...`) cross-referenced against live n8n.io
template pages — direct `n8n.io` fetches are blocked by this sandbox's egress proxy.

**Automation note:** the scheduled `n8n-workflow-scout.yml` and `n8n-brainstorm-scrape.yml`
GitHub Actions (fire ~00:07 UTC and ~07:00 UTC daily) both ran again this morning and both
failed — same root cause every day since 2026-07-20: the Anthropic account's credit balance
behind the shared `ANTHROPIC_API_KEY` repo secret is too low, so neither pipeline gets past
its first Claude call. That's 18+ consecutive failed days. Today's brief below was produced
by hand instead, so the daily cadence held — but the Action needs credits added at
console.anthropic.com to resume running unattended; no further code change will fix it. See
`n8n-workflows/_inbox/2026-08-07-scrape-log.md` for the full raw-pull log and open-PR
backlog.

Each entry below is import-ready as an n8n node chain and priced per the Rung 2 complexity
rubric ($1,500–$2,000 simple / $2,000–$3,500 medium / $3,500–$5,000 complex).

---

## Sales

### 11. Technographic Prospecting: BuiltWith Tech-Stack Signals → Trello Lead Board — *simple*
`Schedule Trigger` → `Google Sheets (target domains)` → `HTTP Request (BuiltWith tech lookup)` → `Code (flag competing-tool/no-automation signal)` → `Trello (create lead card)` → `Google Sheets (mark processed)`
- **Template fit:** new — proposed `SAL-10`. Outbound-by-tech-stack angle none of SAL-B01..B10 cover.
- **Source:** ["Automate Sales Pipeline: BuiltWith Technology Data to Trello Lead Cards with Google Sheets"](https://n8n.io/workflows/4786-automate-sales-pipeline-builtwith-technology-data-to-trello-lead-cards-with-google-sheets/) — live on n8n.io today.

---

## Ecommerce

### 11. AI Product Listing Generator from Photos (Gemini + Airtable) — *medium*
`Google Drive/Airtable Trigger (new photo)` → `Gemini vision (title/description/tags)` → `Airtable (stage for review)` → `If (approved)` → `Shopify (create product)` → `Slack (notify merchandiser)`
- **Template fit:** new — proposed `ECM-10`. First listing-creation card; every other ECM card manages a lifecycle event, not new-SKU setup.
- **Source:** ["Generate Shopify product listings from images with Gemini AI and Airtable"](https://n8n.io/workflows/10008-generate-shopify-product-listings-from-images-with-gemini-ai-and-airtable/) — n8n.io, live today.

---

## Wholesaling / REI

### 11. Conversational AI Property Search Agent (Buyer-Facing) — *medium*
`Chat Widget/WhatsApp Trigger (buyer inquiry)` → `AI Agent (parse budget/area/beds/investor-vs-owner)` → `HTTP Request (Bright Data/MLS search)` → `Code (match buy-box)` → `AI Agent (conversational results)` → `CRM (log buyer + matches)`
- **Template fit:** reinforces `WHL-02` — buyer-facing counterpart to the dispo-matching card.
- **Source:** ["Find your home with Real Estate Agent and Bright Data"](https://n8n.io/workflows/4872-find-your-home-with-real-estate-agent-and-bright-data/) — n8n.io, live today.

---

## Productivity

### 11. Internal Knowledge Base Assistant (Slack + RAG over Drive) — *medium*
`Slack Trigger (@mention/DM)` → `Vector Store retrieval (Drive SOPs/policies/proposals)` → `AI Agent, Claude (grounded answer + citation)` → `If (low confidence)` → `Slack (answer + sources, or escalate)`
- **Template fit:** new — proposed `PRD-10`. First "ask the company anything" card in the vertical.
- **Source:** ["Slack AI chatbot for business team with RAG, Claude 3.7 Sonnet and Google Drive"](https://n8n.io/workflows/3414-slack-ai-chatbot-for-business-team-with-rag-claude-37-sonnet-and-google-drive/) — n8n.io, live today.

---

## Small Business

### 11. Client Onboarding Orchestrator (Contract → Welcome Packet → Task Board) — *medium*
`Webhook (signed contract/deposit)` → `Airtable (create client record)` → `PDF gen (welcome packet)` → `Trello (onboarding checklist board)` → `Gmail (welcome + kickoff link)` → `Slack (notify account owner)`
- **Template fit:** reinforces `SMB-01` — picks up right after intake booking with a structured onboarding flow instead of an ad hoc thread.
- **Source:** ["Streamline client onboarding with PDF, Trello, Slack, Gmail & Airtable"](https://n8n.io/workflows/8930-streamline-client-onboarding-with-pdf-trello-slack-gmail-and-airtable/) — n8n.io, live today.

---

## Quick take for DJ

- **Billing blocker — action needed:** the `ANTHROPIC_API_KEY` behind both scheduled
  Actions has an exhausted/too-low credit balance. It's failed **every day since
  2026-07-20** (18+ days). Add credits at console.anthropic.com — nothing else fixes this.
- **PR backlog:** 6 open draft PRs (#49, #50, #52, #53, #55, #56) are waiting on manual
  review/merge, all green on CI. This run opens a 7th. Recommend batching through them in
  date order so `main` and the open branches stop drifting apart.
- **Fastest close:** Sales #11 (technographic prospecting) is the cheapest, simplest build
  this round — a Rung 1/2 border SDR-team upsell.
- **Best bundle upsell:** Small Business #11 (client onboarding) is a one-call add-on for
  any client already on `SMB-01`; Wholesaling #11 pairs directly with `WHL-02`.
