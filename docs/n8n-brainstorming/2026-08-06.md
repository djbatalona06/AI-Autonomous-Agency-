# n8n Workflow Scout — 2026-08-06

Manual run — both scheduled Actions (`n8n-workflow-scout.yml`, `n8n-brainstorm-scrape.yml`)
have now failed on **every run for 17+ consecutive days** (2026-07-20 through 2026-08-06),
reconfirmed directly against GitHub Actions run history and job logs this session (not just
carried forward from prior notes). Root cause unchanged: the Anthropic account behind
`ANTHROPIC_API_KEY` has an insufficient credit balance. See
`n8n-workflows/_inbox/2026-08-06-scrape-log.md` for full evidence and the run IDs checked.

Direct `n8n.io` scraping is still blocked in this environment too (Cloudflare 403s both a
plain `WebFetch` and a `r.jina.ai` reader-proxy fallback tried for the first time this run),
so today's findings come from targeted web search, cross-checked against live
`n8n.io/workflows/<id>-<slug>/` URLs before being logged — same fallback method used on
every manual run since 2026-07-22.

Each entry below is import-ready as an n8n node chain and priced per the Rung 2 complexity
rubric ($1,500–$2,000 simple / $2,000–$3,500 medium / $3,500–$5,000 complex).

---

## Sales

### 8. Rep Performance Tracker with AI Coaching Insights — *medium*, reinforces SAL-B08
`Schedule Trigger` → `HTTP Request (CRM + telephony/email logs)` → `Code (aggregate per-rep activity)` → `AI Agent (coaching insights + quota-risk flags)` → `Google Sheets (scorecard)` → `Slack (weekly manager digest)`
- **Template fit:** manager-facing coaching tool — SAL-B08 flags individual stalled deals; this scores rep activity/quota trends over time.
- **Source:** ["Track and analyze sales performance with AI insights and Google Sheets"](https://n8n.io/workflows/5975-track-and-analyze-sales-performance-with-ai-insights-and-google-sheets/)

---

## Ecommerce

### 8. Multi-Platform RAG Storefront Assistant (Shopify + WooCommerce) — *medium*, reinforces ECM-B06
`Chat Widget/Webhook` → `Switch (platform)` → `Vector Store (catalog/policy RAG)` → `Shopify/WooCommerce (live order lookup)` → `AI Agent (GPT-4o/Gemini)` → `IF (needs human)` → `Slack/Zendesk escalate`
- **Template fit:** platform-agnostic RAG layer (catalog/policy docs, not just order history) plus native WooCommerce support — fits merchants running both platforms.
- **Source:** ["E-commerce assistant for Shopify & WooCommerce with GPT-4o, Gemini & RAG"](https://n8n.io/workflows/6100-e-commerce-assistant-for-shopify-and-woocommerce-with-gpt-4o-gemini-and-rag/)

---

## Wholesaling / REI

### 8. Automated Property Market Report Generator — *medium*, new candidate
`Schedule Trigger (weekly)` → `HTTP Request (Bright Data comps/trends)` → `AI Agent (market narrative)` → `PDF generation` → `Gmail (send to seller/investor list)` → `Google Sheets (log + tracking)`
- **Template fit:** first content/authority-marketing card in WHL — every other card is transactional. Doubles as a warm-lead nurture touch.
- **Source:** ["Automated property market reports with Bright Data & n8n"](https://n8n.io/workflows/5220-automated-property-market-reports-with-bright-data-and-n8n/)

---

## Productivity

### 8. AI Receipt & Expense Processing Agent — *simple*, new candidate
`Google Drive Trigger` → `VLM Run (extract vendor/date/items/total)` → `Code (categorize)` → `Airtable (log)` → `IF (over threshold → Slack approval)` → `Schedule Trigger (monthly Sheets summary)`
- **Template fit:** first finance/back-office card in PRD — clean low-hours Ops Retainer add-on.
- **Source:** ["Automate receipt processing for expense tracking with Google Drive, VLM Run & Airtable"](https://n8n.io/workflows/8393-automate-receipt-processing-for-expense-tracking-with-google-drive-vlm-run-and-airtable/)

---

## Small Business

### 8. Custom Booking Engine (Calendar + Business Hours + REST API) — *medium*, reinforces SMB-01
`Webhook (REST API)` → `Code (business-hours/buffer rules)` → `Google Calendar (check conflicts)` → `IF (available)` → `Google Calendar (create event)` → `Webhook Response`
- **Template fit:** infrastructure/backend layer, distinct from SMB-01/SMB-05's messaging-layer focus on an existing booking system — for clients with no calendar tooling to build a custom intake form on top of.
- **Source:** ["Complete booking system with Google Calendar, business hours & REST API"](https://n8n.io/workflows/8635-complete-booking-system-with-google-calendar-business-hours-and-rest-api/)

---

## Quick take for DJ

- **Billing blocker, 17+ days running — now the top-priority open item.** Both scheduled
  Actions have failed every day since 2026-07-20 for the same reason (Anthropic account
  credit balance) and three code-level PRs (#42, #48, #49) haven't touched it because it
  isn't a code problem. Recommend adding credits at console.anthropic.com before anything
  else on this routine.
- **Five draft PRs will now be open** (#49, #50, #52, #55, and this run's) once this lands,
  all green, all waiting on manual review. Nothing in the backlog needs a fix — just review
  time.
- **Fastest close:** Productivity #8 (Receipt/Expense Agent) is the cheapest build this
  round — good Ops Retainer filler for any client already on a monthly plan.
- **Distinct/new ground:** Wholesaling #8 (Market Report Generator) is the first
  content-marketing angle in a vertical that's been 100% transactional until now — worth a
  second look as a standalone lead-nurture offer, not just a WHL card.
